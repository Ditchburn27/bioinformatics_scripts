#setting up conda environment for cell oracle 

conda create -n celloracle_env

conda activate celloracle_env

##Setting up Python environment
#Installation of some libraries requires non-default anaconda channels. Add the channels below

conda config --add channels defaults
conda config --add channels bioconda
conda config --add channels conda-forge

#Install dependencies using conda
conda install numba cython pybedtools jupyter notebook pysam

#Install cell oracle and other dependencies 
pip install git+https://github.com/morris-lab/CellOracle.git

#If cell oracle fails due to velocyto
#conda install numpy scipy cython numba matplotlib scikit-learn h5py click pysam llvm louvai
#pip install velocyto

#If cell oracle fails due to scanpy
#conda install scanpy

#Other libraries that may have to be installed 
#conda install pybedtools pyarrow tqdm joblib jupyter gimmemotifs==0.14.4 genomepy==0.8.4

##Setting up R environment 
#Install R in conda environment

conda install r 

#Install required libraries for network analysis

#Activate R in command line and then use 'install.packages()'
R 
install.packages("igraph")
install.packages("linkcomm")
install.packages("https://cran.r-project.org/src/contrib/Archive/rnetcarto/rnetcarto_0.2.4.tar.gz",
                 repos = NULL, type = "source", configure.args = '--host=host')

#If installation of rnetcarto fails try the following 
#Install clang (arm64 M1 mac)
#conda install -c conda-forge clang

#Install rgraph
#conda install -c conda-forge https://anaconda.org/conda-forge/r-ggraph/2.0.5/download/osx-64/r-ggraph-2.0.5-r41h9951f98_0.tar.bz2

